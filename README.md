# Reflect

Reflect is a secure Electron desktop app that creates a local Express mock server from OpenAPI 3.x YAML specifications.

## Development

```sh
yarn install
yarn dev
```

Run `yarn build`, `yarn lint`, `yarn typecheck`, and `yarn test` before submitting changes. See `AGENTS.md` for architecture and contribution guidance.
